LensPrime MVP

Goal:
Upload CSV files, convert them into a local SQLite database, define table relationships, and generate SQL queries through a guided interface.

Version 0.1:
- Import CSV files
- Create SQLite tables
- View table names and columns
- Manually define a relationship between two tables
- Generate a basic SELECT/JOIN/GROUP BY query
- Show results